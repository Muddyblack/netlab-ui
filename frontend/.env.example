# Backend base URL (FastAPI). Defaults to http://localhost:8000
VITE_API_BASE=http://localhost:8000
# Topology file the backend session binds to (relative to backend working dir)
VITE_TOPOLOGY_PATH=lab.yml
